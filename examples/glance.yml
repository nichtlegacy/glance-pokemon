# Example page for glance-pokemon

pages:
  - name: Fun
    columns:
      - size: small
        widgets:
          - $include: widgets/random-pokemon.yml
            options:
              min-id: 1
              max-id: 493
